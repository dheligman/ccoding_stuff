#include <cstdio>

int main() {
    char house[] = "a " 
    "house " 
    "of " "gold.";
    printf("A book holds %s\n ", house);
}
/*When the strings are wrote out separately, the string will be 
recorded with everything combined. This is useful for long strings.
Can write the string in multiple lines to conserve space.*/